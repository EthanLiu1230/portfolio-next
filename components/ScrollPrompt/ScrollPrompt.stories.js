import ScrollPrompt from './ScrollPrompt';
import React from 'react';

export default {
  title: 'ScrollPrompt',
  component: ScrollPrompt
};

const Template = (args) => <ScrollPrompt/>;

export const Default = Template.bind({});

